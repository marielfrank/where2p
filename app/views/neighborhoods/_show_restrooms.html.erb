<!-- close main column, begin nav column -->
<br>
<% if !neighborhood.restrooms.empty? %>
    <%= render partial: 'restrooms/list_restrooms', locals: {restrooms: neighborhood.restrooms} %>
<% elsif logged_in? %>
    Be the first to add a restroom in this neighborhood!<br>
    <%= link_to "Add a new restroom", new_restroom_path, class: 'btn cta' %><br>
<% else %>
    We don't know about any restrooms in <%= neighborhood.name %> yet! Sign in to add one ;)<br>
<% end %>
