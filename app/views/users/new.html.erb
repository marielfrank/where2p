<h1>Sign Up</h1>
<%= render partial: 'form', locals: {user: @user, submit_text: "Sign Up"} %>

<%= render 'sessions/oauth-link' %>

<%= render 'layouts/close_main_open_nav' %>

<h3>Already have an account?</h3>
<%= link_to "Log in", login_path, class: 'btn ghost-cta' %>